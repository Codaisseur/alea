apiVersion: v1
kind: PersistentVolume
metadata:
  name: "{{ .Values.postgres.diskNamePrefix }}-2"
  labels:
    heritage: alea
    name: "{{ .Values.postgres.diskNamePrefix }}-2"
spec:
  capacity:
    storage: {{ .Values.postgres.diskSize }}
  accessModes:
    - ReadWriteOnce
  awsElasticBlockStore:
    volumeID: "{{ .Values.postgres.diskNamePrefix }}-2"
    fsType: "ext4"
