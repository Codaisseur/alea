apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.redis.diskName }}
  labels:
    heritage: alea
    name: {{ .Values.redis.diskName }}
spec:
  capacity:
    stoage: {{ .Values.redis.diskSize }}
  accessModes:
    - ReadWriteOnce
  gcePersistentDik:
    pdName: {{ .Values.redis.diskName | quote }}
    fsType: "ext4"
