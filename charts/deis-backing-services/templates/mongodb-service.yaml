apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  labels:
    app: mongodb-apps
    heritage: deis-backing-services
spec:
  ports:
  - name: mongodb
    port: 27017
    targetPort: mongodb
  selector:
    app: mongodb-service
