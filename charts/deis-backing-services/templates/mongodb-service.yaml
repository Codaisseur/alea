apiVersion: v1
kind: Service
metadata:
  name: coderunner-mongo
  labels:
    app: coderunner-mongo
    heritage: deis-backing-services
spec:
  ports:
  - name: mongodb
    port: 27017
    targetPort: mongodb
  selector:
    app: coderunner-mongo
